"use client";
import { FormEvent, useState } from 'react'; import { useRouter } from 'next/navigation';
export default function AdminLogin(){const [form,setForm]=useState({username:'',password:''});const [error,setError]=useState('');const router=useRouter();async function submit(e:FormEvent){e.preventDefault();const res=await fetch('/api/auth/admin-login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const data=await res.json();if(!res.ok){setError(data.error);return;}router.push('/admin/dashboard');}return <main className="page shell" style={{maxWidth:520}}><a className="brand" href="/">IOTRICITY <span>// CONTROL</span></a><div style={{marginTop:90}}><p className="eyebrow">ORGANIZER ACCESS</p><h1 style={{fontSize:44,margin:"18px 0 12px"}}>Control<br /><span style={{color:"var(--orange)"}}>room.</span></h1><form onSubmit={submit} className="panel" style={{marginTop:30}}><label className="eyebrow">USERNAME</label><input className="input" style={{margin:"10px 0 16px"}} value={form.username} onChange={e=>setForm({...form,username:e.target.value})} required /><label className="eyebrow">PASSWORD</label><input type="password" className="input" style={{margin:"10px 0 16px"}} value={form.password} onChange={e=>setForm({...form,password:e.target.value})} required />{error&&<p className="danger" style={{marginBottom:14}}>{error}</p>}<button className="button" style={{width:"100%"}}>ENTER CONTROL ROOM</button></form></div></main>}