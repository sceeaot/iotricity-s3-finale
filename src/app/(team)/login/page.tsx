"use client";
import { FormEvent, useState } from 'react';
import { useRouter } from 'next/navigation';
export default function Login() { const [code,setCode]=useState(''); const [error,setError]=useState(''); const [busy,setBusy]=useState(false); const router=useRouter(); async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');const res=await fetch('/api/auth/team-login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({teamCode:code})});const data=await res.json();setBusy(false);if(!res.ok){setError(data.error);return;}router.push('/dashboard');} return <main className="page shell" style={{maxWidth:520}}><a className="brand" href="/">IOTRICITY <span>// S03</span></a><div style={{marginTop:80}}><p className="eyebrow">FIELD ACCESS / TEAM NODE</p><h1 style={{fontSize:46,margin:"18px 0 12px"}}>Identify your<br /><span style={{color:"var(--acid)"}}>crew.</span></h1><p className="muted" style={{lineHeight:1.6}}>Enter the code issued to your team to open the current challenge.</p><form onSubmit={submit} className="panel" style={{marginTop:30}}><label className="eyebrow" htmlFor="team-code">TEAM CODE</label><input id="team-code" className="input" style={{margin:"12px 0 16px",textTransform:"uppercase"}} value={code} onChange={e=>setCode(e.target.value)} placeholder="TEAM01" required autoFocus />{error&&<p className="danger" style={{marginBottom:14}}>{error}</p>}<button className="button" style={{width:"100%"}} disabled={busy}>{busy?'CONNECTING...':'OPEN FIELD NODE'}</button></form><a href="/admin/login" className="muted" style={{display:"block",marginTop:24,fontSize:12}}>Organizer access -&gt;</a></div></main> }