"use client";
import { useEffect, useState } from 'react';
type Row={rank:number;teamName:string;coins:number;stagesCompleted:number;componentsRedeemed:number};
export default function Leaderboard(){const [rows,setRows]=useState<Row[]>([]);const [updated,setUpdated]=useState('');async function load(){const res=await fetch('/api/leaderboard');const data=await res.json();setRows(data.leaderboard||[]);setUpdated(new Date().toLocaleTimeString());}useEffect(()=>{load();const id=setInterval(load,5000);return()=>clearInterval(id)},[]);return <><header className="topbar"><div className="shell topbar-inner"><a className="brand" href="/">IOTRICITY <span>// RANKINGS</span></a><nav className="nav"><a href="/dashboard">Console</a><a href="/shop">Shop</a></nav></div></header><main className="page shell fade-in"><div style={{display:'flex',justifyContent:'space-between',alignItems:'end',marginBottom:28}}><div><p className="eyebrow">PUBLIC SIGNAL / LIVE</p><h1 style={{fontSize:42,margin:'12px 0 0'}}>Leaderboard</h1></div><span className="muted" style={{fontSize:11}}>UPDATED {updated}</span></div><div className="panel table-wrap"><table><thead><tr><th>Rank</th><th>Team</th><th>Stages</th><th>Credits</th><th>Parts</th></tr></thead><tbody>{rows.map(row=><tr key={row.rank}><td style={{color:row.rank<=3?'var(--acid)':'var(--muted)',fontSize:row.rank<=3?20:14}}>{String(row.rank).padStart(2,'0')}</td><td style={{fontWeight:row.rank<=3?'bold':'normal'}}>{row.teamName}</td><td>{row.stagesCompleted}/5</td><td>{row.coins} BC</td><td>{row.componentsRedeemed}/4</td></tr>)}</tbody></table>{!rows.length&&<p className="muted" style={{padding:'25px 0'}}>No teams have connected yet.</p>}</div></main></>}